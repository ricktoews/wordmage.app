const AddIcon = props => {
	return <button onClick={props.onClick} className="btn btn-danger badge-add">
          <span className="glyphicon glyphicon-plus"></span>
        </button>
}

export default AddIcon;

